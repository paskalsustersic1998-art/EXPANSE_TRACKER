from fastapi import APIRouter

# Import and include feature routers here as they are built:
# from app.api.routes import auth, trips, expenses, settlements, admin

api_router = APIRouter()

# Placeholder — replace with real routers as features are added
# api_router.include_router(auth.router, prefix="/auth", tags=["auth"])
# api_router.include_router(trips.router, prefix="/trips", tags=["trips"])
